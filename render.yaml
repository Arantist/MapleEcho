services:
  - type: web
    name: audio-separation-backend
    runtime: docker
    rootDir: backend
    plan: starter
    envVars:
      - key: CORS_ORIGINS
        value: http://localhost:3000,https://fengye-rain.life,https://www.fengye-rain.life
